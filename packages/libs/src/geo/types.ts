/**
 * 表示地理坐标点的数据类型
 *
 * 定义为包含两个数字的元组类型：
 *   - 第一个元素为经度（longitude）
 *   - 第二个元素为纬度（latitude）
 *
 * @example
 * const location: point = [116.4074, 39.9042];
 */
export type point = [number, number];
